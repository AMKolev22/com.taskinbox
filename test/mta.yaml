_schema-version: "3.2"
ID: com.test.testProject
version: 0.0.1
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
        - npm run build:workspaces
modules:
  - name: com.test.testProject-ui-deployer
    type: com.sap.application.content
    path: .
    requires:
      - name: com.test.testProject-auth
      - name: com.test.testProject-app-front
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - dist/test-content.zip
          name: test
          target-path: resources/
  - name: test
    type: html5
    path: test
    build-parameters:
      supported-platforms: []
resources:
  - name: com.test.testProject-app-front
    type: org.cloudfoundry.managed-service
    parameters:
      service: app-front
      service-plan: developer
  - name: com.test.testProject-auth
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
