{
    "name": "com.test.testProject",
    "type": "module",
    "scripts": {
        "build": "mbt build --mtar com.test.testProject.mtar",
        "build:workspaces": "npm run build --workspaces --if-present",
        "deploy": "cf deploy mta_archives/com.test.testProject.mtar --retries 0",
        "lint": "npm run lint --workspaces --if-present",
        "ui5lint": "npm run ui5lint --workspaces --if-present",
        "start:test": "npm start --workspace test"
    },
    "dependencies": {
        "@prisma/client": "^6.14.0",
        "@ui5/cli": "^4.0.22",
        "express": "^4.21.2",
        "fastify": "^5.5.0",
        "mbt": "^1"
    },
    "workspaces": [
        "test"
    ],
    "devDependencies": {
        "prisma": "^6.14.0"
    }
}
